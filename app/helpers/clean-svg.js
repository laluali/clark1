import { helper } from '@ember/component/helper';

export function cleanSvg(params/*, hash*/) {
  let svg = null;
  switch(params){
    case 'list_12110962_1':
      ''
      break;
  }
  return params;
}

export default helper(cleanSvg);
